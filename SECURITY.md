# 安全策略

## 支持范围

安全修复优先针对当前主分支和最新发布版本。

## 报告方式

仓库发布到 GitHub 后，请优先使用 GitHub Security Advisories 的私密报告功能。

如果私密报告尚未启用，可以提交一个不包含利用细节的普通 Issue，请维护者建立私密联系。不要公开发布可直接利用的进程控制、命令注入或路径覆盖细节。

## 安全边界

本项目只允许控制自己启动的 `-insecure` 离线 Dota 2 回放进程。任何扩大到联网比赛、任意控制台命令、远程地址、进程注入、VAC 绕过或隐蔽运行的修改均不属于本项目范围。
