# 更新记录

## 1.9.3 - 2026-08-06

- 设置面板新增 Dota 2 程序路径，可粘贴完整路径或浏览选择 `dota2.exe`。
- 用户指定路径只保存在当前电脑的应用数据中，不写入项目、安装包或 GitHub 仓库。
- 保存路径后立即刷新成片能力和推荐录像目录，并可随时恢复原有自动检测。
- 路径配置只替换 Dota 2 程序的定位来源；DEM 分析、镜头方案、离线导出和自动关闭行为保持不变。

## 1.9.2 - 2026-08-05

- 将“普通画面 / 干净画面”提升为工作台右侧始终可见的分段选择，不再藏在“机位”页底部。
- 最近任务侧栏显示全部返回任务，超过可用高度时在侧栏内部滚动。
- 每次分析或重新打开任务后持久记录最后使用时间，最近任务按真实使用顺序刷新。
- 成片、质量报告和源素材目录默认沿用原 `.dem` 文件名；重复导出自动追加 `_2`、`_3`，不覆盖已有输出。
- 导出预检显示预计文件名、普通/干净画面状态和自动编号策略。

## 1.9.1 - 2026-08-05

- FFmpeg、FFprobe、PowerShell 与 Windows 辅助命令统一使用无控制台窗口的后台进程，录制和编码期间不再反复闪出黑色窗口。
- 媒体子进程继承禁止系统错误弹窗的错误模式；遇到 `0xc0000142` DLL 初始化失败时仅受控重试一次，仍失败则回到软件内报告并停止任务。
- 媒体失败信息补充十六进制退出码，便于定位启动故障，不再把所有启动失败都误报为“未安装 FFmpeg”。
- 新增本地原始帧/WAV 重编码回归，无需重新打开 Dota 2 即可验证编码路径。
- 统一导出素材中的“英雄跟随”命名，不再把 Hero Chase 标为真正的英雄近景。

## 1.9.0 - 2026-08-05

- 新增可逆的“默认剪辑 / WTF 导演”双方案工作区；片段、主角、当前选择、画面设置和 WTF 故事来源分别保存。
- 默认剪辑保持击杀、已验证补刀斧砍树、手工片段和精确时间编辑；采用 WTF 故事不再覆盖默认方案。
- 右侧交互拆分为“主角、时间、机位”，WTF 导演额外显示“故事”；当前模式和采用状态始终明确可见。
- `Hero Chase` 更名为“英雄跟随”，明确它是可选备用机位，不再与导演模式或真正近景混为一谈。
- 剪辑方案 schema 升级到 `d2h.edit-plan/1.5`；旧 `1.3/1.4` 单方案自动迁移到默认剪辑。

## 1.8.1 - 2026-08-04

- 修复连续击杀中后死亡目标由更早技能起手时，故事节拍时间倒序并导致整份录像分析失败的问题。
- 保持击杀结果的时间顺序和最终击杀落点，同时稳定处理重叠起手时间，不截断连续击杀。
- 增加倒序起手回归测试，并使用真实录像完成本地全流程分析验证。

## 1.8.0 - 2026-08-03

- 新增版本化的故事、证据、参与者、置信度、节拍、机位素材和建议切换时间窗合同。
- 将米拉娜连续击杀、风行者重复补刀斧砍树和买活再死组织为可解释故事。
- 机位由剧情证据决定；普通场次可只保留玩家视角，关键动作才生成英雄近景备用素材。
- 同场机位使用完全相同的录像时间段，并按 `S001-A`、`S001-B` 等编号导出独立 MP4。
- 默认成片只使用主机位，备用特写不会作为重复片段顺序追加。
- 输出目录新增源素材文件夹与 `素材清单.json`，应用内可直接打开。
- 保留 1.7 手工精确剪辑、米拉娜击杀、风行者 7 次砍树、本地游戏原声和可选更新行为。

## 1.7.0 - 2026-07-29

- 新增击杀、技巧互动、团战和地图目标四轨事件时间轴。
- 支持整局与当前高光主角相关两种显示范围。
- 显示片段覆盖范围，并可从事件直接定位或新增片段。
- 新增由官方 GitHub Releases 提供、经过公钥验签的可选应用内更新。
- 更新仅提示，由用户决定是否下载；分析或导出期间不允许安装。
- 扩充前端时间轴单元测试，完整验证统一运行全部前端测试。

## 1.6.0

- 建立本地 DEM 分析、10 人阵容、高光主角和内容规则筛选。
- 支持连续击杀段与经过证据验证的补刀斧砍树互动。
- 支持精确时间、玩家视角/英雄近景、Dota 2 原生离线导出和 FFmpeg 质量检查。
